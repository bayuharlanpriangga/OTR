# OTR PROJECT STATUS

## Current Phase
Phase 13 — Authentication (belum dimulai — lihat "Next Phase" di bawah)

## Completed
- Struktur folder proyek sesuai Master Spec §3.
- Design tokens (`css/variables.css`): color, typography, spacing, radius, shadow, motion.
- Base styles: `reset.css`, `typography.css`.
- App shell: sidebar (desktop), bottom nav (mobile), topbar, content outlet.
- Komponen reusable: button, card, modal, toast, spinner/skeleton loading, empty state.
- Signature visual: `.weave` (motif lattice ala punggung kartu tarot, CSS murni).
- Router hash-based (`js/router.js`) — static-hosting friendly, tidak butuh server rewrite.
- Global state object (`js/core/state.js`) sesuai shape di Master Spec §5.
- Event bus, storage wrapper, utils.
- 9 halaman skeleton (belum functional): Home, Reading, Daily, Library, History, Journal, Statistics, Settings, Profile.
- `data/tarot-cards.js` — 78 kartu lengkap (22 Major + 56 Minor Arcana), schema sesuai Master Spec §7.
- `data/tarot-keywords.js` — index ringan terpisah dari data kartu penuh.
- `data/default-spreads.js` — 10 spread MVP sesuai katalog Master Spec §13.
- **`js/tarot/shuffle.js`** — `secureRandomInt()`/`secureRandomFloat()` (Web Crypto `crypto.getRandomValues()` dengan rejection sampling anti-bias, fallback `Math.random()`) dan `shuffleDeck()` (Fisher-Yates, Master Spec §10), tidak memutasi array input.
- **`js/tarot/orientation.js`** — `rollOrientation(reversedProbability = 0.5)`, default 50% upright/50% reversed (Master Spec §11), probability bisa dikonfigurasi (mis. untuk fitur "matikan kartu terbalik" di Settings nanti). Helper `isUpright`/`isReversed`.
- **`js/tarot/deck.js`** — `class TarotDeck`: `reset()`, `shuffle()`, `draw()` (throw kalau deck kosong), `clone()`, getter `remainingCount`/`drawnCount`/`drawnCards`/`isEmpty`/`totalCount`. Duplicate prevention otomatis lewat mutasi tumpukan `_remaining` (Master Spec §9).
- **`js/tarot/spreads.js`** — wrapper di atas `data/default-spreads.js`: `resolveSpread(id)` (lookup + validasi, throw kalau spread tidak ada atau `cardCount` tidak cocok `positions.length`), `getPositionAt`, `getPositionById`, `getNextPosition`, `isSpreadComplete`.
- **`js/tarot/draw.js`** — `drawForPosition(deck, position, options)`: fungsi murni yang menggabungkan `deck.draw()` + `rollOrientation()` jadi satu entri `{ positionId, cardId, orientation, card }`.
- **`js/tarot/tarot-engine.js`** — `class TarotEngine` (+ `createTarotEngine()` factory, `READING_STATUS` enum): orkestrasi penuh reading tanpa DOM dan tanpa dependensi ke `js/core/state.js`/`event-bus.js` (sengaja dipisah supaya reusable & gampang dites lewat `node`).
  - API wajib: `createReading()`, `drawCard()`, `completeReading()`.
  - API tambahan: `reshuffle()`, `revealCurrentCard()`, `abortReading()`, `getReading()`, `getSpread()`, `isComplete()`.
  - State machine sesuai Master Spec §15: `idle → setup → shuffling → drawing → revealing → interpreting → (kartu berikutnya kembali ke drawing) → … → completed`. Setiap method punya guard eksplisit (`_assertStatus`) yang throw error jelas kalau dipanggil di luar urutan — mis. `revealCurrentCard()` sebelum `drawCard()`, atau `completeReading()` sebelum semua posisi terisi.
  - Reading entity yang dihasilkan mengikuti schema Master Spec §14 (`id, userId, spreadId, question, intention, status, cards[], summary, createdAt, completedAt`) plus `currentPosition` dari shape state §5, supaya tinggal di-`patchState("reading", engine.getReading())` di Phase 5 tanpa transformasi tambahan.
- Semua file JS Phase 2 & Phase 3 lolos syntax check (`node --check`).
- Runtime test Phase 3 (script sekali-pakai, tidak ikut deliverable): deck 78/78 kartu tanpa duplikat, shuffle mempertahankan isi & mengubah urutan, orientation 50/50 dalam toleransi wajar + bisa dipaksa 100% upright/reversed, seluruh 10 spread di katalog divalidasi (`positions.length === cardCount`), full reading cycle (create → draw → reveal → interpret, berulang → complete) dijalankan end-to-end untuk **semua 10 spread MVP**, dan 4 skenario invalid-state-transition dipastikan throw. Total 202/202 assertion lulus.
- **`css/tarot-card.css`** — styling penuh komponen kartu (sebelumnya placeholder kosong dari Phase 1). Flip 3D murni CSS (`transform-style: preserve-3d` + `backface-visibility: hidden`, digerakkan lewat atribut `data-revealed`), rasio 2:3 lewat `--card-ratio` (variables.css), 3 preset ukuran (`--sm/--md/--lg`) pakai `clamp()` supaya fluid di mobile/tablet/desktop tanpa breakpoint manual. Semua durasi transisi pakai token `--duration-*`, yang otomatis jadi 0ms di bawah `prefers-reduced-motion` (sudah didefinisikan global di `variables.css`) — jadi requirement "Reduced Motion" terpenuhi tanpa media query tambahan di file ini. Card back memakai `.weave` (signature visual yang sudah ada). Orientation reversed memutar seluruh isi wajah kartu 180° lewat `.tarot-card__face-content`, sementara badge "Terbalik" (pakai `.badge--reversed` yang sudah ada) sengaja diletakkan di luar elemen itu supaya tetap terbaca tegak. State loading memakai overlay yang reuse keyframe `.skeleton` dari `components.css` (tidak duplikasi animasi).
- **`js/components/tarot-card.js`** — dua API: `tarotCardHTML(opts)` (markup string statis, cocok untuk render batch mis. grid 78 kartu di Library Phase 9) dan `renderTarotCard(container, opts)` (mount + wire interaksi klik/keyboard, mengembalikan handle imperative `reveal()/hide()/setOrientation()/setSelected()/setDisabled()/setLoading()/update()/destroy()` — dipakai Reading flow Phase 5 untuk mengatur timing reveal terpisah dari draw). Menerima data kartu mentah (schema §7) + flag orientation/revealed/selected/disabled/loading/interactive — **tidak** mengimpor `js/tarot/tarot-engine.js` sama sekali, jadi komponen ini reusable di luar konteks reading (Library, History, dsb) tanpa bawa-bawa state machine reading. Label eyebrow ("MAJOR ARCANA · 00", "CUPS · SEVEN") memakai `ARCANA_LABELS`/`SUIT_LABELS`/`RANK_LABELS` yang sudah ada di `data/tarot-keywords.js` — tidak menulis ulang mapping label. Semua teks dinamis (nama kartu, dll) di-escape sebelum masuk `innerHTML`.
- Semua file JS Phase 4 lolos syntax check (`node --check`).
- Runtime test Phase 4 (script sekali-pakai, pakai `jsdom`, tidak ikut deliverable): markup statis untuk kondisi revealed/unrevealed/reversed/slot-kosong, mount + klik mouse, aktivasi keyboard (Enter & Space memicu, tombol lain tidak), kartu non-interactive tidak merespons klik, seluruh method imperative (`reveal/setOrientation/setSelected/setLoading/setDisabled/destroy`) diverifikasi mengubah `data-*` attribute yang benar. Total 27/27 assertion lulus.
- **Bugfix (ditemukan awal Phase 5, sebelum kode Phase 5 ditulis): folder `tarot/` (deck.js, shuffle.js, orientation.js, spreads.js, draw.js, tarot-engine.js) ternyata ada di root repo, bukan di `js/tarot/` seperti yang diwajibkan Master Spec §3.** Semua import relatif di dalam modul-modul itu (mis. `"../../data/tarot-cards.js"`, `"../core/utils.js"`) ditulis dengan asumsi lokasi `js/tarot/` — jadi di lokasi root, import tersebut sebetulnya resolve ke luar folder repo dan akan gagal begitu file-file itu benar-benar di-load lewat `<script type="module">`/dynamic `import()` di browser (runtime test Phase 3 lolos karena dijalankan lewat `node` dengan path absolut manual, bukan lewat resolusi relatif sesungguhnya seperti browser — jadi bug ini tidak ketahuan sampai sekarang). Kemungkinan besar penyebabnya proses upload manual ke GitHub yang menjatuhkan prefix `js/`. Diperbaiki dengan `git mv tarot/*.js js/tarot/` (riwayat file tetap terjaga) — tidak ada satu baris import pun yang perlu diubah, karena source code-nya memang sudah benar untuk lokasi `js/tarot/`, cuma foldernya yang salah tempat. Diverifikasi lewat smoke test Node (`createReading → drawCard → revealCurrentCard → completeReading` end-to-end) sebelum lanjut ke Phase 5.
- **`js/pages/reading.js`** — full rewrite (sebelumnya skeleton Phase 1). Mengimplementasikan seluruh Reading flow (Master Spec §16–20, Roadmap Phase 5): Pilih Jenis (One Card/Three Card) → Pilih Spread → Pertanyaan/Niat/Kategori → Shuffle → (Tarik Kartu → Reveal → Interpretasi) berulang per posisi → selesai. Halaman ini murni "lem" (page controller): membuat `TarotEngine` baru tiap kali `render()` dipanggil, memanggil API wajibnya (`createReading`, `drawCard`, `completeReading`) plus `reshuffle`/`revealCurrentCard`/`abortReading`, dan meng-mount `renderTarotCard()` (Phase 4) untuk tahap draw — tidak ada logic tarot baru ditulis di sini, semuanya delegasi ke engine Phase 3.
  - Setiap error dari guard `TarotEngine` (`_assertStatus`) ditangkap lewat try/catch di titik pemanggilan (`drawCard()`, `revealCurrentCard()`) dan ditampilkan sebagai toast, bukan meng-crash halaman — konsisten dengan Master Spec §15 ("Do not allow users to skip into invalid states") karena UI memang tidak pernah menawarkan tombol yang membuat state invalid, tapi tetap ada jaring pengaman kalau ada race/bug.
  - Kategori (general/love/career/spiritual) dipilih user di step Pertanyaan (default dari `spread.category`, fallback ke `general` kalau kategori spread bukan salah satu dari 4 itu, mis. `daily`) — dipakai untuk memilih field mana dari `card.upright`/`card.reversed` yang ditampilkan saat interpretasi per kartu.
  - Shuffle screen (Master Spec §17) render 5 elemen kartu-belakang statis (`.shuffle-deck__card--1..5`, style `.weave`) yang di-animasikan lewat keyframe CSS individual (`animations.css`) saat tombol "Kocok Lagi"/otomatis di awal step ditekan — tiap klik juga memanggil `engine.reshuffle()` sungguhan (bukan animasi kosmetik semata). Durasi tunggu sebelum tombol "Tarik Kartu" aktif kembali dipersingkat drastis kalau `prefers-reduced-motion: reduce` aktif.
  - Draw screen (Master Spec §18–19): kartu face-down (`renderTarotCard`, size `lg`, `interactive:true`) bisa diklik langsung atau lewat tombol "Tarik Kartu" — keduanya memanggil `handleDraw()` yang sama. Ada jeda singkat (dipersingkat ke 0 di reduced-motion) antara `drawCard()` dan `.reveal()` supaya flip tidak terasa "menyatu" dengan aksi tarik.
  - Form Pertanyaan/Niat/Kategori dibaca lewat `FormData(form)`, bukan shorthand akses properti-bernama (`form.question.value`) — walau shorthand itu valid di semua browser nyata, `FormData` lebih eksplisit dan (kebetulan) juga yang membuat halaman ini bisa dites otomatis lewat `jsdom` (jsdom tidak mengimplementasikan shorthand itu).
  - `abortReading()` dipanggil lewat modal konfirmasi ("Batalkan reading?", pakai `openModal` yang sudah ada) yang muncul saat tombol "Batal" ditekan di step Shuffle/Draw — mencegah reading batal tidak sengaja karena sekali klik.
- **`js/pages/result.js`** (baru) — Result Page (Master Spec §24). Murni membaca `getState().reading` (di-patch oleh `reading.js` saat `completeReading()`) + `getSpreadById`/`getCardById` untuk resolve data penuh dari id yang tersimpan di reading entity — tidak import `TarotEngine` maupun menyentuh `js/tarot/` sama sekali, sesuai pola pemisahan yang sudah dipakai sejak Phase 3/4.
  - Kalau tidak ada reading yang `status: "completed"` di state (mis. akses langsung `#/result` tanpa lewat flow, atau reading di-reset), tampilkan `emptyStateHTML` dengan CTA ke `#/reading` — tidak pernah crash karena data kosong/tidak lengkap.
  - Per kartu: nama, badge orientasi, keyword chips, teks makna (`card.upright`/`card.reversed[category]`, fallback ke `.general`), dan nasihat (`.advice`) — semuanya field yang sudah ada di `data/tarot-cards.js` sejak Phase 2, ditampilkan apa adanya (bukan hasil "engine" baru).
  - "Reading Baru" berfungsi penuh (`resetReading()` + `navigate("/reading")`). "Simpan Reading" dan "Tulis Journal" sengaja dibuat `disabled` dengan `title` yang menjelaskan fase mana yang akan mengaktifkannya (Phase 8 — Local Storage, Phase 11 — Journal) — bukan tombol mati tanpa penjelasan.
- **`css/reading.css`** — styling penuh untuk Reading flow & Result page (sebelumnya placeholder kosong). Shuffle deck, draw stage, interpretation panel, layout kartu di Result page.
- **`css/animations.css`** — 5 keyframe (`shuffle-fan-1..5`) untuk animasi shuffle (sebelumnya placeholder kosong). Otomatis netral di bawah `prefers-reduced-motion` lewat safety-net global di `reset.css` (Phase 1) — tidak perlu media query tambahan di sini.
- `.gap-5` ditambahkan ke `layout.css` (dipakai `reading.js`/`daily.js` tapi belum pernah didefinisikan — kemungkinan besar `daily.js` skeleton Phase 1 sudah pakai class ini secara prematur/typo, baru ketahuan sekarang karena Reading MVP benar-benar mem-render-nya).
- Route `/result` didaftarkan di `js/app.js` dan `js/config.js` (`ROUTES`) — dipakai `app-shell.js` untuk resolve judul topbar.
- Semua file JS lolos syntax check (`node --check`), termasuk seluruh isi `js/tarot/` di lokasi barunya.
- Runtime test Phase 5 (script sekali-pakai, pakai `jsdom`, tidak ikut deliverable): full flow Three Card (pilih jenis → pilih spread → isi pertanyaan+kategori → shuffle → tarik+reveal+lanjut x3 → lihat hasil), verifikasi state global ke-patch benar (`status`, jumlah kartu, `spreadId`, `category`), verifikasi navigasi hash ke `#/result`, verifikasi Result page merender 3 kartu revealed + pertanyaan + nama spread, verifikasi "Reading Baru" me-reset state & navigasi balik, verifikasi Result page menampilkan empty state kalau tidak ada reading, dan sanity check jalur One Card (spread 1 kartu langsung ke tombol "Lihat Hasil" tanpa tombol "Kartu Berikutnya"). Total 54/54 assertion lulus.
- **`js/tarot/interpretation.js`** (baru — Master Spec §21–23). Interpretation Engine deterministik, bukan AI: dua fungsi murni, tanpa DOM/state, konsisten dengan pola `tarot-engine.js`/`deck.js` (testable lewat `node` saja).
  - `interpretCard({ card, orientation, position, category, question })` → `{ title, keywords, meaning, advice, reflection }` (Master Spec §21). `meaning` di-resolve lewat rantai prioritas 4-tier persis Master Spec §22: **1) position-specific** (titik ekstensi `card.positionMeanings?.[position.id]?.[orientation]?.[category]` — belum ada satu pun kartu di `data/tarot-cards.js` yang punya field ini, jadi tier ini saat ini selalu jatuh ke tier berikutnya; lihat "Important Decisions") → **2) category-specific** (`card.upright/reversed[category]`, dilewati kalau category `"general"` karena itu tier 4) → **3) orientation-specific** (`card.upright/reversed.general`, sudah orientation-aware lewat pemilihan bucket) → **4) fallback keras** (2 kalimat generik hardcoded, beda untuk upright/reversed) yang menjamin "never leave the result empty" walau data kartu rusak/tidak lengkap.
  - `advice` pakai rantai serupa tapi lebih pendek (`card.upright/reversed.advice`, fallback ke 1 kalimat generik) — schema kartu tidak punya advice per-kategori.
  - `reflection` (field baru, tidak ada padanannya di `data/tarot-cards.js`) dibangun dari bank template per kategori (general/love/career/spiritual), dipilih **deterministik** (hash string sederhana dari `cardId:positionId:orientation`, bukan `Math.random()`) supaya input yang sama selalu menghasilkan output yang sama persis — properti penting untuk "engine", bukan generator teks acak. Kalau ada `question`, ditambahkan sebagai kalimat penutup singkat.
  - `synthesizeReading({ entries, category, question })` → `{ theme, dominantKeywords, keyMessage, reflection, cards }` (Master Spec §23). `dominantKeywords` = keyword lintas semua kartu, diurutkan berdasarkan frekuensi kemunculan (lalu alfabetis untuk tie-break), dibatasi 5. `theme` = kalimat template dari 3 keyword teratas + label kategori. `keyMessage` = **advice dari kartu di posisi terakhir** — heuristik sengaja sederhana (posisi terakhir di spread naratif linear macam past→present→future atau situation→obstacle→advice biasanya yang paling actionable/ke depan), bukan pemilihan "cerdas" berbasis makna; didokumentasikan sebagai heuristik, bukan diklaim sebagai natural-language understanding.
  - Semua field dijamin non-empty (tidak ada `""`/`null` yang lolos ke output) sesuai requirement "Never leave the result empty" di Master Spec §22.
- **`js/pages/reading.js` disambungkan ke Interpretation Engine** — panel interpretasi per kartu (muncul setelah "Lanjutkan") sekarang menampilkan `meaning`/`advice`/`reflection` hasil `interpretCard()`, bukan lagi lookup field mentah manual (`pickMeaning`/`pickAdvice` versi Phase 5 dihapus). `finishReading()` sekarang memanggil `synthesizeReading()` dan memakai `synthesis.theme` sebagai `reading.summary` (field string di Reading Entity, Master Spec §14) — bukan lagi gabungan keyword mentah.
- **`js/pages/result.js` disambungkan ke Interpretation Engine** — per kartu memanggil `interpretCard()` (menampilkan meaning/advice/reflection lengkap termasuk reflection yang sebelumnya tidak ada sama sekali di Phase 5), dan bagian "Overall Theme/Key Message/Reflection" sekarang benar-benar hasil `synthesizeReading()`, bukan lagi ringkasan sementara berlabel "akan disempurnakan Phase 6" — label disclaimer itu sudah dihapus karena sudah tidak relevan.
- Runtime test Phase 6 (2 script sekali-pakai, tidak ikut deliverable):
  - `test_phase6_interpretation.mjs` (pure Node, tanpa DOM) — kontrak output 5 field, rantai prioritas tier 1–4 (termasuk tier 1 yang jatuh ke tier 2 saat tidak ada data, dan tier 1 yang benar-benar dipakai saat data ekstensi ada), never-empty guarantee dengan kartu data minimal/rusak, determinisme (`interpretCard`/`synthesizeReading` dipanggil 2x dengan input sama → output identik byte-per-byte), validasi error saat `card`/`orientation` tidak diberikan, `keyMessage` = advice posisi terakhir (termasuk kasus one-card), `dominantKeywords` ranking berdasarkan frekuensi, `reflection` sintesis menyertakan `question` kalau ada. Total 29/29 assertion lulus.
  - `test_phase6_flow.mjs` (`jsdom`) — full flow Three Card end-to-end lewat `reading.js` sampai `result.js`, memverifikasi panel interpretasi tersembunyi sampai "Lanjutkan" ditekan lalu terisi teks substantif, `reading.summary` berisi kalimat theme yang sebenarnya (bukan keyword mentah), dan Result page menampilkan ketiga section sintesis + disclaimer "sementara" Phase 5 sudah hilang. **Test ini yang menangkap bug integrasi nyata** sebelum sempat terkirim — lihat Known Issues.

## In Progress
- (tidak ada — Phase 12 selesai & terverifikasi, Phase 13 belum mulai coding, lihat "Next Phase")

## Not Started
- Semua phase 13–27. Satu blocker sebelum Phase 13 mulai coding: `supabase/migrations/0002_seed_system_data.sql` (baru sesi ini) belum dijalankan terhadap Supabase asli — lihat Known Issues & Next Phase.

## Current Architecture
- Vanilla HTML/CSS/JS, ES Modules, tanpa framework/build step.
- Routing: hash-based (`#/reading`, dst), didaftarkan di `js/app.js` via `registerRoute()`.
- State: satu object global di `js/core/state.js`, diakses via `getState()`/`setState()`/`patchState()`.
- Komunikasi antar modul lewat `js/core/event-bus.js` (event: `route:change`, `toast:show`, `state:change`).
- Halaman = modul dengan `export default { render(container, params) }`, di-load lazy lewat dynamic `import()`.
- Data domain tarot independen dari UI (`data/`), dikonsumsi Tarot Engine (`js/tarot/`) tanpa DOM sama sekali.
- **Tarot Engine (`js/tarot/`) juga independen dari state management/UI** — `tarot-engine.js` tidak meng-import `js/core/state.js` atau `event-bus.js`. Layer di atasnya (`js/pages/reading.js`, Phase 5) yang menyambungkan hasil `engine.getReading()` ke `patchState("reading", ...)`. Keputusan ini menjaga engine tetap testable murni lewat `node` tanpa DOM/browser — terbukti berguna waktu memverifikasi bugfix lokasi folder di awal Phase 5 (lihat Known Issues).
- `js/tarot/` sekarang benar-benar berada di `js/tarot/` (dipindah dari root repo di awal Phase 5 — lihat Completed & Known Issues), sesuai Master Spec §3.
- **`js/tarot/interpretation.js` (Phase 6) mengikuti pola independensi yang sama** — fungsi murni, tidak import `state.js`/`event-bus.js`, tidak tahu apa itu `TarotEngine`. Dipanggil langsung oleh page controller (`reading.js` & `result.js`), sejajar dengan `tarot-engine.js` — bukan lapisan di atas/di bawahnya. `TarotEngine` menangani *fisik* reading (deck, draw, status), `interpretation.js` menangani *makna* dari kartu yang sudah ditarik; keduanya tidak saling import.
- Struktur folder & nama file mengikuti Master Spec §3 persis.

## Important Decisions
- Router pakai hash (`#/...`), bukan History API — supaya kompatibel static hosting tanpa rewrite rule.
- Font: Fraunces (display) + Public Sans (body) + IBM Plex Mono (data/angka) via Google Fonts CDN.
- Palet warna "night-reading table": obsidian/plum sebagai ruang, gold sebagai foil-accent, oxblood untuk state reversed/destructive.
- Data Minor Arcana (56 kartu) di-generate dari kombinasi tema suit × tema rank/court, bukan ditulis satu-satu manual. Teks Major Arcana (22 kartu) ditulis individual.
- `tarot-keywords.js` sengaja dipisah dari `tarot-cards.js` supaya fitur search/filter (Phase 9) & quiz (Phase 21) bisa jalan tanpa nge-load seluruh teks upright/reversed 78 kartu.
- `image` path memakai format `/assets/images/tarot/{suit-or-major}/{slug}.webp` — placeholder, asetnya diisi di Phase 4.
- **Randomization pakai `crypto.getRandomValues()` dengan rejection sampling** (bukan modulo langsung) supaya distribusi shuffle/orientation tidak bias ke angka kecil — sesuai Master Spec §10. Fallback ke `Math.random()` hanya kalau Web Crypto API benar-benar tidak tersedia di environment.
- **`TarotEngine` sengaja dipisah dari `TarotDeck`**: `deck.js` cuma tahu soal kartu fisik (draw/shuffle/reset), tidak tahu apa itu "reading" atau "spread". `tarot-engine.js` yang mengorkestrasi deck + orientation + spread jadi satu reading entity. Pemisahan ini supaya tiap modul gampang dites sendiri-sendiri (lihat runtime test Phase 3).
- **Reveal dipisah jadi state tersendiri dari draw** (`drawing` → `revealing` → `interpreting`), bukan digabung jadi satu langkah — mengikuti state machine Master Spec §15 dan mencegah UI (Phase 5) "skip" langsung dari draw ke interpretasi tanpa render animasi reveal.
- **`drawCard()` mengembalikan objek kartu penuh** (`card`, hasil resolve dari `getCardById`), tapi yang disimpan permanen ke `reading.cards` hanya `{ positionId, cardId, orientation }` — konsisten dengan schema Reading Entity Master Spec §14 (tidak menyimpan data kartu yang sudah redundan dengan `data/tarot-cards.js`).
- **`js/components/tarot-card.js` sengaja tidak tahu apa-apa soal `TarotEngine`** — komponen cuma nerima props data kartu + flag state. Pemisahan ini konsisten dengan pola yang sudah dipakai di Phase 2 & 3 (data/logic independen dari UI): Phase 5 nanti yang jadi "lem" antara `engine.drawCard()` dan `renderTarotCard()`.
- **Reversed orientation memutar seluruh wajah kartu 180°** (bukan cuma teks atau cuma artwork) — meniru tampilan kartu tarot fisik yang benar-benar terbalik, bukan sekadar ikon "reversed" di kartu yang tetap tegak. Badge kecil di pojok (tidak ikut rotasi) yang menjaga keterbacaan status orientasi.
- **Background art kartu pakai CSS `background-image` di sebuah `<div>`, bukan `<img>`** — karena `image` path di `data/tarot-cards.js` masih placeholder (aset `.webp` belum ada, lihat Known Issues Phase 2/3). Dengan `background-image`, gambar yang gagal load tidak memunculkan broken-image icon; huruf awal nama kartu (`.tarot-card__art-fallback`) selalu jadi fallback yang terlihat sengaja, bukan rusak. Begitu aset asli tersedia, tidak perlu ubah komponen — tinggal isi file di path yang sudah direferensikan.
- **`js/pages/reading.js` sengaja membuat `TarotEngine` baru tiap kali `render()` dipanggil** (bukan singleton module-level yang dipertahankan lintas navigasi) — navigasi ke `#/reading` = mulai reading baru, konsisten dengan mental model "reading" sebagai sesi sekali jalan. State reading yang **sudah selesai** dipertahankan lewat `js/core/state.js` (bukan lewat instance engine), supaya Result Page bisa dibaca ulang tanpa bergantung ke module reading.js.
- **Kategori interpretasi (general/love/career/spiritual) dipilih di step Pertanyaan, bukan otomatis dari spread** — walau tiap spread punya `category` default (dipakai sebagai nilai awal `<select>`), user tetap bisa override. Diteruskan apa adanya ke `interpretCard()`/`synthesizeReading()` (Phase 6) sebagai parameter `category`.
- **(Supersedes catatan Phase 5)** Result Page sekarang benar-benar memakai `synthesizeReading()` untuk "Overall Theme/Key Message/Reflection" — bukan lagi ringkasan sementara. Disclaimer "Interpretation Engine penuh menyusul di Phase 6" sudah dihapus dari UI karena sudah tidak akurat.
- **"Simpan Reading" dan "Tulis Journal" di Result Page dibuat `disabled` dengan `title` yang menyebutkan fase aktivasinya** (Phase 8, Phase 11) — bukan disembunyikan begitu saja — supaya user tahu fitur itu memang direncanakan, bukan lupa dikerjakan.
- **Form Pertanyaan/Niat/Kategori dibaca lewat `FormData(form)`**, bukan `form.<name>.value` — lebih eksplisit, dan (efek samping yang disengaja dimanfaatkan) membuat halaman ini testable lewat `jsdom` tanpa workaround.
- **Prioritas makna Master Spec §22 diimplementasikan penuh 4 tier walau tier 1 ("position-specific") saat ini tidak pernah punya data untuk dipakai** — pilihan sadar, bukan disederhanakan jadi 3 tier. Alasannya: (a) tetap sesuai kontrak spec apa adanya, (b) jadi titik ekstensi nyata kalau suatu saat ada kebutuhan makna per-posisi yang authored (mis. "The Fool di posisi Future" beda dari "The Fool di posisi Past"), dan (c) diuji eksplisit di runtime test Phase 6 supaya perilaku fallback-nya terverifikasi, bukan cuma diasumsikan.
- **`reflection` (field baru di output `interpretCard`) dipilih dari bank template lewat hash string deterministik, bukan `Math.random()`** — konsekuensinya: reflection prompt yang sama akan selalu muncul untuk kombinasi kartu+posisi+orientasi yang sama. Ini trade-off yang disengaja: engine yang benar-benar deterministik (gampang dites, hasilnya reproducible) lebih penting untuk MVP daripada variasi teks yang terasa "hidup" tapi tidak predictable — variasi lewat randomness asli bisa ditambahkan nanti kalau dianggap perlu, tanpa mengubah kontrak fungsi.
- **`synthesizeReading()`'s `keyMessage` = advice dari kartu di posisi TERAKHIR**, bukan gabungan semua advice atau pemilihan berbasis makna semantik — heuristik paling sederhana yang tetap masuk akal untuk spread naratif linear yang ada sekarang (past→present→future, situation→obstacle→advice, dst., di mana posisi terakhir umumnya paling actionable). Didokumentasikan sebagai heuristik yang mungkin perlu direvisi kalau spread non-linear (mis. Mind/Body/Spirit yang tidak punya urutan naratif jelas) ditambahkan nanti.
- **`js/pages/reading.js` dan `js/pages/result.js` masing-masing memanggil `interpretCard()`/`synthesizeReading()` sendiri-sendiri saat render**, tidak saling berbagi hasil lewat state — `reading.js` memakai hasilnya untuk tampilan sementara (panel per kartu) dan untuk `reading.summary` (`synthesis.theme`, satu string, sesuai schema Master Spec §14); `result.js` menghitung ulang dari `reading.cards`/`spreadId`/`category` yang tersimpan. Konsisten dengan pola "page membaca id lalu resolve data" yang sudah dipakai sejak Phase 5 — dan karena `interpretCard`/`synthesizeReading` murni & cepat (bukan network call), menghitung ulang jauh lebih sederhana daripada menyinkronkan cache hasil lewat state.

**Phase 7 — Result Experience** (Roadmap Phase 7, Master Spec §24). Tidak ada data/logic baru — `interpretCard()`/`synthesizeReading()` dipanggil dengan cara persis sama seperti Phase 6. Seluruh perubahan murni presentasi di `js/pages/result.js` (rewrite struktural, bukan tambal inline style) + `css/reading.css` (bagian "Result page" yang tadinya stub 12 baris) + `css/animations.css` (satu keyframe baru) + `js/components/icons.js` (2 ikon baru).
- **Hero header** — eyebrow "Reading Selesai", nama spread, kutipan pertanyaan (kalau ada), dan timestamp "Diselesaikan pada …" dari `reading.completedAt` (field yang sudah ada sejak `tarot-engine.js` Phase 3, belum pernah ditampilkan di UI manapun sampai sekarang).
- **Card list dengan "position rail"** — tiap kartu di-render dengan nomor posisi (lingkaran, `font-mono`) + garis vertikal penghubung ke kartu berikutnya (dihilangkan otomatis di kartu terakhir), supaya urutan naratif spread (mis. Situation → Challenge → Advice) terbaca sekilas tanpa harus membaca teks satu-satu. `position.description` (Master Spec §12, sudah ada di `data/default-spreads.js` sejak Phase 2 tapi belum pernah dipakai UI) sekarang ditampilkan sebagai caption kecil di bawah nama posisi.
- **Panel sintesis ditonjolkan** — Overall Theme/Key Message/Reflection sekarang satu `.result-synthesis` card dengan border gold-dim, watermark `.weave` transparan (memakai signature visual yang sudah ada, bukan elemen dekoratif baru), dan pembatas internal antar block — menandakan visual bahwa ini kesimpulan dari kartu-kartu di atasnya, bukan section yang sejajar.
- **`.result-divider`** — ornamen garis tipis dengan bidang berlian kecil di tengah (CSS murni, gradient + pseudo-element), menggantikan `<hr>` generik yang dipakai di Phase 5/6.
- **Tombol aksi memakai ikon** (`sparkle` untuk "Reading Baru", `bookmark` baru untuk "Simpan Reading", `feather` untuk "Tulis Journal") — pola `icon()` yang sebelumnya cuma dipakai di sidebar/bottom-nav, sekarang dipakai pertama kali di tombol. Kedua tombol yang masih `disabled` (Simpan Reading, Tulis Journal) tetap mempertahankan `title` fase aktivasi dari Phase 6.
- **Reveal beruntun per section** (`.result-section`, keyframe `result-reveal` di `animations.css`) — 4 section (header, card list, sintesis, aksi) masuk dengan fade+translateY yang di-stagger lewat `animation-delay` per `:nth-of-type`. Otomatis `animation: none` di bawah `prefers-reduced-motion: reduce` (ditulis eksplisit di `reading.css`, bukan cuma mengandalkan `--duration-slow: 0ms` dari token global) — safety-net ganda supaya delay yang "kerasa" tanpa gerakan tidak lolos.
- Semua file JS lolos syntax check (`node --check`).
- Runtime test Phase 7 (script sekali-pakai, pakai `jsdom`, tidak ikut deliverable, `test_phase7_result.mjs`): full flow Three Card end-to-end (create → draw+reveal×3 → complete → patchState → render), memverifikasi 4 `.result-section` & 3 `.result-divider` ter-render, header menampilkan nama spread/pertanyaan/timestamp yang benar, jumlah `.result-card-entry` = jumlah posisi, nomor rail berurutan, garis penghubung hilang di kartu terakhir, caption deskripsi posisi muncul sesuai data spread, panel sintesis punya watermark + 3 block, 3 tombol aksi ter-render dengan ikon dan status disabled yang benar, klik "Reading Baru" me-reset state, empty state ter-render kalau tidak ada reading `completed`, dan pertanyaan dengan karakter `<script>`/`&`/`"` ter-escape dengan benar (tidak ada raw `<script>` di `innerHTML`, tapi teksnya tetap terlihat sebagai text content). Ditambah sanity check terpisah untuk jalur One Card (1 `.result-card-entry`, 0 garis penghubung, tidak ada blok pertanyaan kalau pertanyaan kosong). Total 24/24 assertion lulus (23 flow Three Card + 1 log gabungan One Card).

**Bugfix (ditemukan lewat smoke test manual pertama di browser sungguhan, pasca-Phase 7): kartu tidak animasi flip sama sekali — langsung terbuka tanpa transisi.** Root cause di `js/components/tarot-card.js` (Phase 4), bukan CSS: `reveal()`/`hide()` men-set `data-revealed` lalu langsung memanggil `rerender()` (`container.innerHTML = tarotCardHTML(state)`), yang mengganti seluruh elemen kartu dengan elemen DOM baru yang *sudah* lahir dalam state akhirnya. Karena browser tidak pernah melihat perubahan atribut pada elemen yang sama di antara dua frame, CSS transition di `.tarot-card__inner` (`tarot-card.css`) tidak punya apa pun untuk di-animasikan — kartu langsung snap ke hasil akhir. Ini bug yang sudah ada sejak Phase 4/5 tapi lolos dari seluruh runtime test `jsdom` sebelumnya karena test-test itu cuma memverifikasi atribut akhir (`data-revealed === "true"`, dst), bukan memverifikasi identitas elemen DOM sebelum & sesudah — jsdom juga tidak mengeksekusi CSS transition sama sekali, jadi kelas bug ini secara struktural tidak mungkin ketahuan lewat `jsdom`, cuma lewat browser sungguhan.
  - Diperbaiki dengan membuat `reveal()`/`hide()` memutasi elemen yang sudah ter-mount secara langsung (`dataset.revealed`, `aria-label` via `setAttribute`) alih-alih memanggil `rerender()`.
  - Badge orientasi "Terbalik" (sebelumnya cuma masuk markup kalau `revealed && orientation === "reversed"`, yang berarti butuh rerender penuh untuk muncul) sekarang selalu ada di DOM saat `orientation === "reversed"` — visibilitasnya sebelum reveal tetap otomatis tersembunyi lewat mekanisme `backface-visibility: hidden` yang sama yang menyembunyikan seluruh wajah depan kartu (badge itu child dari `.tarot-card__front`), bukan lewat kondisi markup. Atribut `aria-hidden` di-toggle manual saat reveal/hide supaya screen reader tidak membaca teks "Terbalik" sebelum kartu benar-benar dibuka.
  - `setOrientation()`/`setDisabled()`/`update()` tetap memakai `rerender()` — aman karena ketiganya dipanggil di luar momen animasi flip (sebelum kartu di-reveal, atau mengganti kartu yang masih tertutup).
  - Ditambahkan `buildAriaLabel()` sebagai helper bersama, dipakai baik oleh `tarotCardHTML()` (di-escape untuk HTML) maupun `reveal()`/`hide()` (raw, lewat `setAttribute` yang tidak butuh escape) — supaya logic label tidak duplikat di dua tempat dan bisa saling konsisten.
  - Diverifikasi lewat `test_reveal_flip_fix.mjs` (`jsdom`, one-off, tidak ikut deliverable): 18/18 assertion, termasuk assertion inti "`handle.el` sebelum dan sesudah `reveal()` adalah elemen DOM yang sama (`===`)" — cek yang sebelumnya tidak pernah ditulis di test manapun dan yang seharusnya menangkap kelas bug ini kalau ada di masa depan.

**Phase 8 — Local Storage** (Roadmap Phase 8, Master Spec §48). Fokus scope sesuai Requirements/DONE WHEN Roadmap Phase 8 (reading bisa disimpan, bertahan lewat reload, dibuka lagi, dihapus) — bukan seluruh 4 domain yang disebut di Tasks List secara penuh: Journal (Phase 11) & Favorites (Phase 16) baru dapat plumbing storage generik, belum UI.
- **`js/core/storage.js`** — diperluas dari skeleton Phase 1 (`readJSON`/`writeJSON`/`isAvailable`, sudah aman try/catch). Ditambah 4 storage key sesuai Roadmap: `otr_guest_readings` (CRUD penuh: `listGuestReadings`/`getGuestReadingById`/`saveGuestReading` upsert-by-id/`deleteGuestReading`/`setGuestReadingFavorite` skeleton Phase 16), `otr_guest_journal` (`listGuestJournalEntries`/`saveGuestJournalEntries`, skeleton Phase 11), `otr_settings` (`getSettings`/`saveSettings`, merge dengan default), `otr_favorites` (`listFavorites`/`saveFavorites`, skeleton Phase 16).
- **Immutability historical reading (Master Spec §32)** — `js/pages/result.js` sekarang menghitung `interpretCard()`/`synthesizeReading()` **satu kali** per render (sebelumnya dihitung ulang di dalam `cardEntryHTML()` per kartu tanpa disimpan), hasilnya dipakai baik untuk markup maupun untuk `buildSavedReading()` yang membekukan `interpretationSnapshot` per kartu + `synthesisSnapshot` ke dalam record yang disimpan `saveGuestReading()`. `storage.js` sendiri tidak pernah meng-import `js/tarot/interpretation.js` — begitu tersimpan, satu-satunya cara membaca reading itu lagi (`js/pages/history-detail.js`) wajib pakai snapshot beku, tidak pernah memanggil `interpretCard()`/`synthesizeReading()` ulang.
- **Tombol "Simpan Reading" di Result Page** (sebelumnya permanen `disabled` dengan `title="Tersedia di Phase 8"`) sekarang aktif: klik → `saveGuestReading()` → toast sukses → tombol jadi `disabled` + label berubah "Tersimpan" (idempotent; kalau halaman Result dibuka lagi untuk reading yang sama, `getGuestReadingById()` dicek saat render supaya state tombolnya langsung benar tanpa perlu klik ulang).
- **`js/pages/history.js`** — rewrite penuh dari skeleton Phase 1 (yang cuma nampilin empty-state statis): list reading tersimpan (terbaru duluan), tiap item nampilin nama spread/pertanyaan/jumlah kartu/kategori/tanggal selesai, klik → `#/history/:readingId`, tombol hapus per item dengan modal konfirmasi (`openModal`, pola sama seperti `confirmCancel` di `reading.js`). Search/filter/sort tetap ditunda ke Phase 10 sesuai komentar skeleton aslinya.
- **`js/pages/history-detail.js` (baru)** — route `#/history/:readingId`. Reuse penuh class CSS `.result-*` dari Phase 7 (`.result-page`, `.result-header`, `.result-cards`, `.result-card-entry`, `.result-synthesis`, dst) supaya reading yang baru selesai vs. yang dibuka lagi dari History terasa satu bahasa visual — bedanya cuma sumber data (live `interpretCard()` vs `interpretationSnapshot` beku) dan action band (tanpa "Reading Baru"/"Simpan", diganti "Kembali ke History" + "Hapus Reading"). Data kartu penuh (`getCardById()`) tetap di-resolve ulang untuk visual (art/eyebrow) karena itu data referensi statis id-based, bukan bagian dari interpretasi yang wajib beku — hanya teks interpretasi yang dijamin tidak dihitung ulang.
- **Reduced Motion jadi toggle sungguhan** (sebelumnya badge "Segera" permanen di `js/pages/settings.js`, satu-satunya baris Settings yang eksplisit disebut Roadmap Phase 8 — dan satu-satunya phase di seluruh 27 phase Roadmap yang menyentuh Settings sama sekali, jadi sengaja tidak ditunda):
  - `js/core/utils.js` — `prefersReducedMotion()` baru, gabungan OS-level `matchMedia("(prefers-reduced-motion: reduce)")` **DAN** toggle manual (`document.documentElement.dataset.reducedMotion === "true"`). `js/pages/reading.js` yang sebelumnya punya definisi lokal (OS-only) sekarang pakai versi shared ini — mana pun yang aktif, dianggap "kurangi animasi".
  - Toggle manual diterapkan lewat atribut `data-reduced-motion` di `<html>`: di-set saat boot (`js/app.js`, dari `getSettings()`, sebelum router pertama render) dan saat toggle diubah (`js/pages/settings.js`, langsung tanpa reload).
  - 3 companion CSS rule ditambahkan, masing-masing mirror 1:1 blok `@media (prefers-reduced-motion: reduce)` yang sudah ada sejak Phase 1/7, memakai selector atribut `html[data-reduced-motion="true"]` sebagai ganti media query: `reset.css` (safety-net universal `*, *::before, *::after { animation-duration:0.001ms !important; ... }`), `variables.css` (nol-in token `--duration-fast/base/slow`), `reading.css` (`.result-section { animation:none }` eksplisit). Karena hampir semua CSS lain konsumsi lewat `var(--duration-*)`, cuma 3 file ini yang perlu disentuh — bukan setiap file yang punya `animation`/`transition`.
- Runtime test Phase 8 (script sekali-pakai, pakai `jsdom`, tidak ikut deliverable, `test_phase8_storage.mjs`, 51/51 assertion lulus, 6 kelompok):
  1. `storage.js` round-trip: save/get/list/delete/upsert (bukan duplikat), urutan terbaru-duluan, `deleteGuestReading` return `false` kalau id tidak ada (bukan silent no-op), `getSettings`/`saveSettings` merge dengan default, `setGuestReadingFavorite`.
  2. `result.js` save flow: tombol mulai enabled dengan label "Simpan Reading", klik → tersimpan tepat 1 record, tombol jadi disabled + label "Tersimpan", record punya `interpretationSnapshot` non-kosong di semua kartu + `synthesisSnapshot`, render ulang halaman yang sama langsung menampilkan status "Tersimpan" tanpa perlu klik lagi.
  3. `history.js` list/delete: item muncul dengan data yang benar, link `#/history/:id` benar, delete membuka modal konfirmasi, konfirmasi menghapus dari storage sungguhan, list re-render ke empty-state setelah item terakhir dihapus.
  4. `history-detail.js` immutability (**test paling penting phase ini**): simpan reading dengan teks snapshot yang sengaja diberi penanda unik (`TEKS_BEKU_DARI_MASA_LALU_YANG_TIDAK_BOLEH_BERUBAH`) yang tidak mungkin dihasilkan `interpretCard()` — verifikasi teks itu muncul persis apa adanya di halaman detail, membuktikan halaman ini benar-benar membaca snapshot dan bukan menghitung ulang. Plus not-found state, dan delete-dari-detail-page redirect ke `#/history`.
  5. `settings.js` toggle: checkbox reflect state tersimpan, toggle → persist ke storage + langsung ter-apply ke `<html>` tanpa reload, render ulang halaman menampilkan checkbox yang sudah tercentang.
  6. `prefersReducedMotion()`: kombinasi OS+manual — true kalau salah satu aktif.
- Regresi: `test_phase7_result.mjs`/`test_onecard.mjs` (yang sekarang tidak lolos begitu saja karena `result.js` butuh `localStorage` — ditambahkan `global.localStorage` di kedua script, dan asersi "Save button disabled" diperbarui jadi "Save button enabled" karena perilakunya memang legitimately berubah di Phase 8) serta `test_reveal_flip_fix.mjs` dijalankan ulang: total 24+18+51 = 93/93 assertion lulus, nol regresi.
- **`js/pages/library.js`** — rewrite penuh dari skeleton Phase 1 (yang cuma nampilin empty-state statis). Murni UI di atas data yang sudah lengkap sejak Phase 2 (`data/tarot-cards.js`), tidak ada data baru ditambahkan, sama seperti pola Phase 7/9 lainnya. Template penuh cuma dibangun sekali; grid + counter di-update lewat `innerHTML` pada node `[data-library-grid]`/`[data-result-count]` saja (bukan re-render seluruh container) supaya fokus & posisi kursor di input search tidak hilang tiap kali user mengetik.
  - Search (debounced 200ms via `debounce()` dari `js/core/utils.js`) — cocok terhadap nama, slug, atau salah satu keyword kartu (case-insensitive substring).
  - Filter Arcana (Semua/Major/Minor) dan Suit (Semua/Wands/Cups/Swords/Pentacles) lewat `<select>`, mengikuti pola styling `<select>` yang sama dengan Category select di `reading.js` Setup Screen. Suit select otomatis `disabled` + di-reset ke "Semua Suit" saat Arcana = Major (karena Major Arcana tidak punya suit).
  - Grid pakai `.tarot-card-grid` (class yang sudah eksplisit disiapkan di `tarot-card.css` sejak Phase 4 khusus untuk fase ini) + `tarotCardHTML()` dipanggil statis untuk render batch 78 kartu (`revealed:true, interactive:true, size:"sm"`), sesuai anjuran komentar aslinya di `tarot-card.js` ("cocok untuk render batch, mis. grid 78 kartu di Library").
  - Klik/keyboard (Enter atau Space) pada kartu di-tangani lewat **event delegation** yang dipasang di `container` (node stabil, tidak ikut diganti tiap kali grid di-update) — bukan lewat `renderTarotCard()` per-kartu, karena 78 handler individual tidak perlu untuk sekadar navigasi. Klik/Enter/Space → `navigate('/library/:cardId')`.
  - Empty state (`emptyStateHTML()`) muncul kalau kombinasi search+filter tidak menghasilkan kartu apa pun.
- **`js/pages/card-detail.js` (baru)** — route `#/library/:cardId`. Nama file mengikuti file tree Master Spec §3 (`js/pages/card-detail.js`), bukan pola `history-detail.js`, karena halaman ini bukan "hasil reading" (tidak perlu reuse class `.result-*`) melainkan materi referensi/ensiklopedia murni — styling-nya sendiri di `css/library.css`.
  - Struktur mengikuti mockup Master Spec §30 persis: art kartu besar (`tarotCardHTML(..., size:"lg", revealed:true`, orientation upright saja karena ini referensi bukan hasil draw), nama, eyebrow "Major Arcana · 00" / "Wands · Ace" (dihitung dari `ARCANA_LABELS`/`SUIT_LABELS`/`RANK_LABELS` di `data/tarot-keywords.js`), keyword badges, lalu dua section **Upright** & **Reversed** yang masing-masing menampilkan 4 field: General, Love, Career, Spiritual.
  - Field `advice` di data **sengaja tidak ditampilkan** di halaman ini — Master Spec §30 cuma minta 4 field itu per orientasi; `advice` sudah punya tempatnya sendiri lewat `interpretCard()` di Reading flow.
  - Empty/not-found state (reuse `emptyStateHTML()`, pola sama seperti `history-detail.js`) kalau `cardId` di URL tidak valid, dengan tombol kembali ke `#/library`.
- **`js/app.js`** — tambah `registerRoute("/library/:cardId", () => import("./pages/card-detail.js"))` di atas `registerRoute("/history", ...)`.
- **`css/library.css`** — diisi dari placeholder kosong: styling toolbar search/filter (termasuk stacking vertikal + select full-width di breakpoint mobile), dan layout Card Detail (`.card-detail__header`, `.card-detail__meanings` grid 2 kolom → 1 kolom di mobile, divider ornamental yang sengaja diduplikasi — bukan diimpor — dari pola `.result-divider` di `reading.css`, supaya `library.css` tetap self-contained mengikuti konvensi "satu file CSS per domain halaman" yang sudah dipakai proyek ini).
- Runtime test Phase 9 (script sekali-pakai, pakai `jsdom`, tidak ikut deliverable):
  - `test_phase9_library.mjs`, 24/24 assertion lulus, 11 kelompok: render awal 78 kartu, search match ("fool" → 1 hasil, `major_00`) & no-match (0 hasil + empty state), filter Arcana=Major (22 kartu + suit select ter-disable), filter Arcana=Minor+Suit=Wands (14 kartu, semua id berawalan `wands_`), klik kartu → hash berubah ke `#/library/:cardId` yang benar, keyboard Enter → navigasi sama, halaman detail untuk Major Arcana (`major_00`: h1 "The Fool", eyebrow "Major Arcana · 00", 4 badge keyword, section Upright & Reversed ada, 8 meaning block total), halaman detail untuk Minor Arcana (`wands_01`: eyebrow menampilkan "Wands · Ace"), not-found state untuk `cardId` invalid, dan integrity check seluruh 78 kartu resolve lewat `getCardById()`.
  - `test_phase9_routing.mjs`, 3/3 assertion lulus: `#/library` memuat `library.js`, `#/library/major_00` memuat `card-detail.js` (bukan salah match ke route `/library`), dan navigasi balik ke `#/library` memuat ulang `library.js` — membuktikan dua route tidak saling tabrakan di router hash-based.
  - Total 27/27 assertion lulus. `node --check` dijalankan ulang di seluruh 35 file JS proyek (bukan cuma file yang diubah) — nol syntax error, nol regresi terdeteksi (test Phase 1–8 tidak dijalankan ulang di sesi ini karena tidak ada file yang disentuh di luar `library.js`, `card-detail.js`, `app.js`, `library.css`).

### Phase 10 — Reading History (Roadmap Phase 10 / Master Spec §31)
- **`js/pages/history.js` (rewrite)** — Phase 8 sudah menyambungkan halaman ini ke `storage.js` (list, delete, buka detail); fase ini menambahkan yang eksplisit ditunda: Search, Filter, Sort, Favorite. `history-detail.js`/`storage.js` tidak disentuh — `setGuestReadingFavorite(id, isFavorite)` sudah disiapkan sejak Phase 8, tinggal dipakai.
  - **Filter** (Master Spec §31, katalog persis): All, Today, This Week, This Month, Favorites. Today/Week/Month dihitung dari `completedAt` (fallback `createdAt` → `savedAt`) dibanding batas kalender lokal — awal minggu dipatok Senin (konvensi Indonesia, bukan Minggu). Favorites baca `reading.isFavorite`.
  - **Search** — cocok terhadap `spreadName`, `question`, dan nama kartu (`cards[].cardName`) sekaligus, case-insensitive, debounce 200ms mengikuti pola `library.js` Phase 9.
  - **Sort** — Roadmap cuma minta "Sort" tanpa merinci arah; dipilih Terbaru/Terlama (satu-satunya sumbu sort yang masuk akal untuk reading card yang cuma punya field tanggal, bukan mis. nama/abjad). Default tetap Terbaru, konsisten dengan urutan `listGuestReadings()` sejak Phase 8.
  - **Favorite** — tombol star per item (ikon baru `star` di `icons.js`), toggle lewat event delegation di container. Klik langsung memanggil `setGuestReadingFavorite()` (persist ke storage) lalu mutasi objek lokal di memori supaya list ter-update tanpa baca ulang `localStorage`.
  - Render strategy disamakan dengan `library.js`: template penuh (header + toolbar) dibangun sekali; cuma `[data-history-list]` yang di-update tiap search/filter/sort berubah, supaya fokus di search box tidak hilang saat mengetik. Toolbar cuma dirender kalau ada minimal 1 reading tersimpan.
  - Dua empty-state berbeda: "Belum ada reading tersimpan" (0 reading total, sama seperti Phase 8) vs. "Reading tidak ditemukan" (ada reading, tapi search/filter tidak match — pola sama seperti `library.js`).
- **`js/components/icons.js`** — tambah ikon `star` (5-titik, closed path, stroke-only default — konsisten dengan ikon lain di set ini).
- **`css/components.css` (tambahan)** — styling toolbar search/filter/sort (pola sama seperti `.library-toolbar`/`.library-filters` di `library.css`) dan tombol favorit ("diisi" fill emas lewat class `.is-favorite`, bukan dua ikon terpisah untuk state on/off) digabung ke `components.css`, **bukan** file baru. Catatan koreksi: draft awal fase ini sempat bikin `css/history.css` + link-nya sendiri di `index.html` — itu keliru, karena Master Spec §3 cuma daftar 12 file persis di `css/` dan `history.css` bukan salah satunya (`history.js`/`history-detail.js` memang ada di `js/pages/` per spec, tapi styling-nya tidak didedikasikan file sendiri). Sudah di-revert: `css/history.css` dihapus, link di `index.html` dicabut, isinya dipindah ke `components.css`.
- Runtime test Phase 10 (script sekali-pakai, jsdom, tidak ikut deliverable): 31/31 assertion lulus, 9 kelompok — empty state (0 reading, toolbar tidak dirender), render awal + urutan default newest-first, search (cocok nama kartu & question, termasuk kasus tanpa hasil), filter Favorites, filter Today/Week/Month (batas kalender diverifikasi terhadap tanggal berjalan), sort Terlama, favorite toggle (persist ke storage + toggle balik), delete (regresi — masih berfungsi setelah rewrite), dan kombinasi search+filter bersamaan. `node --check` dijalankan ulang di seluruh file JS proyek — nol syntax error, nol regresi.

### Phase 11 — Journal (Roadmap Phase 11 / Master Spec §25-26)
- **`js/core/storage.js`** — Journal di-extend dari skeleton (Phase 8) ke CRUD penuh:
  - `getGuestJournalByReadingId(readingId)`, `saveGuestJournalEntry({ readingId, content })` (upsert **by `readingId`**, bukan by journal id sendiri — §25: "One reading may have one journal entry in MVP"; entry lama dipertahankan id & `createdAt`-nya, cuma `content`+`updatedAt` yang berubah), `deleteGuestJournalEntry(id)`.
  - `deleteGuestReading(id)` di-extend supaya ikut menghapus journal entry terkait (cascade) — mereplikasi manual `journals.reading_id references readings(id) on delete cascade` dari §43 (guest storage tidak punya FK sungguhan). Kontrak lama (`true`/`false` tergantung reading ketemu/tidak) tidak berubah — sudah diverifikasi lewat regression test Phase 10 (lihat bawah).
- **`js/components/journal-editor.js` (baru)** — komponen `JournalEditor` (masuk daftar reusable component di Master Spec §54). Beda dengan SearchInput/Select di `history.js` (Phase 10, sengaja dibiarkan inline karena cuma 1 use-site), `JournalEditor` diekstrak jadi file sendiri karena benar-benar dipakai di 2 halaman (`result.js` utk create, `journal.js` utk edit) — memenuhi bar ekstraksi yang sama persis yang dicatat di Known Issues Phase 10. Cuma textarea polos + Save/Cancel, sesuai §26: "Keep editor simple. Do not build a full Notion clone."
- **`js/pages/result.js`** — tombol "Tulis Journal" yang sejak Phase 7 `disabled` dengan title "Tersedia di Phase 11" sekarang berfungsi penuh:
  - **Keputusan penting**: tombol digerbang di belakang "Simpan Reading" — hanya aktif setelah reading tersimpan (baik baru diklik saat ini, atau memang sudah tersimpan sebelumnya). Ini BUKAN auto-save; kalau reading belum disimpan, tombol `disabled` dengan title "Simpan reading dulu sebelum menulis journal". Alasan: diagram core user flow di Master Spec (dekat §82) eksplisit menunjukkan `... → SAVE → JOURNAL (OPTIONAL) → HISTORY` — SAVE mendahului JOURNAL sebagai langkah terpisah, dan §25 `journal.readingId` perlu menunjuk ke reading yang benar-benar ada di storage (kalau tidak, journal tidak akan pernah bisa dibuka lagi lewat mana pun, termasuk link balik dari `/journal`).
  - Klik "Tulis Journal" (saat enabled) membuka `JournalEditor` inline di bawah action row (tanpa navigasi/route baru — spec cuma punya 1 route `/journal`, tanpa sub-route per-reading). Simpan → `saveGuestJournalEntry()`, toast "Journal disimpan.", tombol berubah jadi `disabled` label "Journal Tersimpan" (mengedit lagi cuma lewat `/journal`, bukan di sini — konsisten dengan pola "Simpan Reading" → "Tersimpan" yang sudah ada).
  - Turut menghormati kasus reading yang sudah punya journal saat halaman ini di-render ulang (`getGuestJournalByReadingId` dicek di awal) — tombol langsung tampil "Journal Tersimpan" tanpa perlu klik apa pun.
- **`js/pages/journal.js` (rewrite dari skeleton Phase 1)** — 3 dari 4 fitur Roadmap yang belum ada (Create ada di `result.js` di atas):
  - **View** — daftar semua journal entry, terbaru (`updatedAt`) duluan. Tiap entry me-resolve reading terkait lewat `getGuestReadingById(entry.readingId)` untuk menampilkan nama spread + pertanyaan (journal sendiri cuma simpan `readingId`, bukan salinan konteksnya — pola sama seperti `history-detail.js` me-resolve data kartu dari id). Nama spread/pertanyaan jadi link balik ke `#/history/:readingId`.
  - **Edit** — tombol per entry, membuka `JournalEditor` inline (pre-filled) tepat di bawah entry itu, Simpan memanggil `saveGuestJournalEntry()` yang sama (upsert by readingId), toast "Journal diperbarui."
  - **Delete** — modal konfirmasi (pola identik `confirmDelete()` di `history.js`), toast "Journal dihapus."
  - **Sengaja TIDAK ada Search/Filter/Sort** — beda dari `history.js` (Phase 10): Roadmap Phase 11 "Features" cuma daftar Create/Edit/Delete/View, tidak menyebut Search/Filter/Sort sama sekali (beda dari Phase 10 yang eksplisit minta ketiganya + Master Spec §31). Kalau nanti dibutuhkan, gampang ditambah mengikuti pola `history.js` yang sudah ada.
  - Defensif terhadap journal entry yatim (reading terkait sudah tidak ada) — walau cascade delete di atas seharusnya mencegah ini terjadi dari alur normal aplikasi, render tetap tidak crash dan menampilkan fallback "Reading tidak ditemukan" (diverifikasi lewat test, lihat bawah).
  - Empty state persis Master Spec §65 (diterjemahkan): "Refleksimu akan muncul di sini setelah kamu menyimpan journal entry pertama."
- **`css/journal.css`** — diisi dari placeholder kosong (file ini SUDAH ada di daftar 12 file `css/` Master Spec §3 sejak awal, beda dengan kasus `history.css` Phase 10 yang keliru — lihat Known Issues Phase 10 di atas soal itu). Isinya minimal: line-clamp 3-baris untuk preview konten di list, dan `min-height` textarea editor.
- Runtime test Phase 11 (script sekali-pakai, jsdom, tidak ikut deliverable): 33/33 assertion lulus, 8 kelompok — full flow Result (gerbang Simpan Reading → Tulis Journal → validasi textarea kosong ditolak → tersimpan → tombol berubah "Journal Tersimpan"), tampil di Journal list dengan konteks reading yang benar, Edit (content+updatedAt berubah, id & createdAt dipertahankan — bukan bikin entry baru), Cancel edit (tidak mengubah apa pun), Delete (+ empty-state setelah entry terakhir), cascade delete (hapus reading ikut hapus journal-nya), defensif terhadap entry yatim, dan empty state saat 0 entry. Plus regression test terpisah untuk Phase 10 (`history.js`) memverifikasi `deleteGuestReading()` yang diperluas tidak mengubah kontrak lama (return `true`/`false`, delete tanpa journal terkait tetap normal) — 8/8 assertion lulus. `node --check` bersih di seluruh file JS proyek.

### Bugfix (di luar penomoran phase, antara Phase 11 & 12) — tombol "Tarik Kartu" di draw step tidak berfungsi
- **`js/pages/reading.js`** — `[data-draw-btn]` di `showDrawStep()` cuma dipakai buat toggle `.disabled`, tidak pernah punya click listener sendiri. Satu-satunya cara narik kartu sebelumnya adalah klik kartu face-down-nya (`renderTarotCard` sudah punya `onClick` sendiri). Sekarang keduanya (klik tombol ATAU klik kartu) memanggil `handleDraw()` yang sama.
- Nama tombol **tetap** "Tarik Kartu" (sempat diusulkan diganti "Buka") — Master Spec §18 (Draw Screen) mockup-nya: "Draw Card" → klik → kartu langsung terungkap dalam 1 aksi, bukan 2 step terpisah (tarik lalu buka terpisah).
- Diverifikasi `drawCard()` di `tarot-engine.js` sudah ada state-machine guard yang menolak `drawCard()` ganda dengan throw (ke-catch, toast error) — 2 entry point (tombol + kartu) tidak menimbulkan race condition baru.
- Test (jsdom, throwaway): integrasi penuh Pilih Tipe → Pilih Spread → Submit Pertanyaan → Shuffle → klik tombol Tarik Kartu di draw step → verifikasi kartu benar-benar tertarik & tombol Lanjutkan muncul. 8/8 assertion lulus.

### Phase 12 — Supabase Foundation (Roadmap Phase 12 / Master Spec §36-48) — ✅ SELESAI, diverifikasi Orias terhadap Supabase asli
**Update sesi ini**: Orias sudah menjalankan migration + `npm run test:supabase` terhadap project Supabase asli miliknya sendiri. `js/config.js` sekarang berisi `SUPABASE_URL`/`SUPABASE_ANON_KEY` asli (bukan placeholder lagi — dikonfirmasi lewat repo). Roadmap Phase 12 DONE WHEN ("Supabase CRUD berjalan dengan RLS") dianggap terpenuhi. Catatan history di bawah ini (kondisi SEBELUM diverifikasi) dipertahankan sebagai jejak proses, bukan status yang masih berlaku.

**Gap yang ditemukan sesi ini, ditutup sebagai bagian penyelesaian Phase 12 (bukan pernah eksplisit disebut di Roadmap Phase 12, tapi tanpa ini Phase 12 secara struktural tidak lengkap)**: `0001_init_schema.sql` membuat tabel `tarot_cards` & `spreads` tapi tidak pernah mengisinya. Karena `readings.spread_id` dan `reading_cards.card_id` adalah foreign key ke dua tabel itu, **setiap insert ke `readings`/`reading_cards` akan gagal FK constraint selama tabel itu kosong** — termasuk fitur inti Phase 13 (Guest Migration) dan seluruh Phase 14 (Cloud Sync) nanti. Ditambahkan:
- **`scripts/generate-seed-sql.mjs`** (baru, dev-only) — generate SQL insert dari `data/tarot-cards.js` + `data/default-spreads.js` langsung (bukan ditulis manual satu-satu, supaya tetap 1 source of truth kalau data kartu/spread berubah). `on conflict do nothing` di semua insert, aman dijalankan ulang.
- **`supabase/migrations/0002_seed_system_data.sql`** (baru, hasil generate) — 78 baris `tarot_cards`, 10 baris `spreads`, 28 baris `spread_positions`, semua `is_system=true` (public read-only, §46).
- **⚠️ Migration `0002` ini BELUM dijalankan terhadap Supabase asli** (ditulis sesi ini, setelah Orias terakhir menjalankan `0001`) — beda dari sisa Phase 12 yang sudah diverifikasi Orias. **Orias perlu menjalankan `0002_seed_system_data.sql` di Supabase Dashboard (SQL Editor) sebelum mencoba fitur Guest Migration di Phase 13**, atau migrasi guest reading akan gagal di step insert `readings` dengan FK violation.
- **Cara migrasi data posisi (`spread_positions`)**: kolom `id`-nya `uuid default gen_random_uuid()`, bukan slug seperti `positions[].id` di `data/default-spreads.js` (mis. `"past"`, `"present"`). Sengaja TIDAK dipetakan manual slug→uuid di seed — kode Phase 13 nanti (`migration-service.js`) resolve posisi lewat `(spread_id, position_index)` yang sudah ada di kedua sisi (local `position.index` dan kolom `position_index`), bukan lewat id/slug.

- **`supabase/migrations/0001_init_schema.sql`** (baru) — 9 tabel PERSIS daftar Roadmap Phase 12 (profiles, tarot_cards, spreads, spread_positions, readings, reading_cards, journals, favorites, daily_cards), disalin dari `CREATE TABLE` di Master Spec §37-45. Plus RLS policies untuk semua tabel (§46: user cuma bisa akses data sendiri; `tarot_cards` & `spreads` yang `is_system=true` public read-only). Plus 1 trigger `handle_new_user()` (auto-insert baris `profiles` saat user baru sign up, `security definer`) — **tidak ada di spec**, ditambahkan karena tanpa ini tabel `profiles` tidak akan pernah terisi (spec sengaja tidak kasih policy insert manual untuk `profiles`, supaya user tidak bisa insert baris dengan id sembarangan).
  - **Catatan penyimpangan dari §36**: §36 "Recommended tables" overview list juga menyebut `user_settings`, tapi TIDAK PERNAH ada `CREATE TABLE`-nya di spec manapun, dan Roadmap Phase 12 sendiri tidak menyebutnya di daftar 9 tabel — kemungkinan besar inkonsistensi di spec, bukan sesuatu yang sengaja ditunda. Sengaja tidak dibuat.
- **`index.html`** — tambah import map (`@supabase/supabase-js` → CDN esm.sh) karena proyek ini murni ES Modules native browser tanpa bundler (Master Spec §4) — tidak ada `node_modules` di app yang di-deploy ke GitHub Pages.
- **`js/config.js`** — tambah `SUPABASE_URL`/`SUPABASE_ANON_KEY` (Master Spec §66: cuma 2 ini yang boleh nyampe ke frontend). **Masih placeholder** (`YOUR-PROJECT-REF`/`YOUR-SUPABASE-ANON-KEY`) — Orias perlu isi manual dengan kredensial project sendiri, lihat `supabase/README.md`.
- **`js/integrations/supabase.js`** (baru) — client singleton lazy (baru benar-benar dibuat saat pertama dipanggil, bukan saat import — banyak halaman, mis. Library/offline reading, sama sekali tidak butuh Supabase). Lempar error eksplisit & jelas kalau config masih placeholder (sengaja, biar ketahuan dari awal, bukan gagal diam-diam di tengah request).
- **`js/services/auth-service.js`** (baru) — **file pertama yang benar-benar masuk `js/services/`** (folder ini ada di Master Spec §3 sejak awal tapi sengaja dilewati terus Phase 1-11 — semua CRUD hidup langsung di `js/core/storage.js`). Auth dipilih sebagai yang pertama karena benar-benar lintas-fase (dibutuhkan Phase 12 buat verifikasi pipeline, Phase 13 buat Login/Register UI, Phase 14 buat cloud sync) — beda dari reading/journal CRUD yang masing-masing sudah punya "rumah" jelas. Cuma primitif: `signUpWithEmail`, `signInWithEmail`, `signOut`, `getSession`, `getCurrentUser`, `onAuthStateChange`. **Tidak ada UI apa pun** (Login/Register page) — itu scope Phase 13, sesuai instruksi eksplisit Roadmap "Jangan implement seluruh UI sync sekaligus".
- **`scripts/supabase-smoke-test.mjs`** (baru, dev-only, bukan bagian app yang di-deploy) — membuktikan pipeline PERSIS urutan Roadmap: Connect → Authenticate → Insert → Select → Update → Delete, terhadap tabel `readings`, plus 1 bonus RLS sanity check (select tanpa login harus kosong/ditolak). **Harus dijalankan manual oleh Orias** dengan kredensial asli — lihat `supabase/README.md` buat cara pakainya, termasuk cara menangani kasus email confirmation Supabase yang aktif secara default di project baru.
- **`package.json`+`package-lock.json`** (baru, root) — dependency Node **dev-only** (`@supabase/supabase-js`, buat smoke-test script). **Tidak memengaruhi app yang di-deploy ke GitHub Pages** — app-nya sendiri tetap murni static file, tidak pernah menjalankan `npm`/bundler apa pun (lihat komentar di file ini sendiri).
- **`.gitignore`** (baru) — `node_modules/`, `.env*`. Belum pernah dibutuhkan sebelum ini karena tidak ada dependency Node yang di-commit sampai fase ini.
- **`supabase/README.md`** (baru) — instruksi setup lengkap: jalankan migration → isi kredensial → jalankan smoke test. Baca ini dulu sebelum menganggap Phase 12 selesai.
- Verifikasi yang SUDAH dilakukan (sandbox, tanpa network ke Supabase): `node --check` bersih di seluruh file JS proyek (termasuk file baru); `npm install` beneran berhasil resolve `@supabase/supabase-js@2.112.x` dari registry asli; API-shape test (throwaway, dengan `fetch` di-intercept supaya tidak butuh network) — 10/10 assertion lulus, membuktikan semua nama method SDK yang dipakai (`auth.signUp`, `auth.signInWithPassword`, `auth.signOut`, `auth.getSession`, `auth.onAuthStateChange`, `.from().insert().select().single()`) benar-benar ada & bisa dipanggil tanpa `TypeError`; guard placeholder config diverifikasi throw dengan pesan yang benar.
- Verifikasi yang **BELUM** dilakukan (butuh Orias): migration SQL belum pernah dijalankan di project Supabase asli manapun (jadi belum ketahuan kalau ada typo SQL/urutan `create table` yang salah karena foreign key ke tabel yang belum ada, dsb — sudah diurutkan hati-hati sesuai dependency §37→§45 tapi belum teruji); RLS policies belum pernah diuji terhadap data & user asli; auth flow (signUp/signIn beneran, termasuk kasus email confirmation) belum pernah dicoba; smoke-test script belum pernah dijalankan sampai selesai.

## Known Issues
- **`supabase/migrations/0002_seed_system_data.sql` belum dijalankan terhadap Supabase asli** (lihat detail di section Phase 12 pada Completed di atas) — **blocking** untuk fitur Guest Migration Phase 13: tanpa ini, insert ke `readings`/`reading_cards` akan gagal FK constraint karena `spreads`/`tarot_cards` masih kosong di database. Jalankan lewat Supabase Dashboard → SQL Editor sebelum mencoba migrasi guest data.
- **Folder `tarot/` duplikat di root repo** (identik isinya dengan `js/tarot/`) — bug ini sebelumnya sudah pernah dicatat & "diperbaiki" lewat `git mv` di Phase 5 (lihat entri Bugfix Phase 5 di atas), tapi sesi ini ditemukan foldernya sudah muncul lagi di root. Kemungkinan besar kebawa lagi tiap proses upload manual ke GitHub (drag-and-drop folder lokal yang masih menyertakan `tarot/` lama). Tidak memengaruhi app yang jalan (semua import sudah benar-benar resolve ke `js/tarot/`), tapi sebaiknya dihapus dari repo supaya tidak membingungkan sesi berikutnya — cukup `git rm -r tarot/` lalu commit, tidak perlu perubahan kode apa pun.
- **Journal cuma bisa ditulis dari Result page (Phase 11), tidak dari History Detail** — kalau reading sudah tersimpan lalu ditinggal tanpa nulis journal, satu-satunya cara nulis reflection untuk reading itu belakangan adalah lewat Result page-nya (yang tergantung `js/core/state.js` masih menyimpan reading itu — akan hilang begitu reading baru dimulai). `history-detail.js` belum punya tombol "Tulis Journal" sendiri. Roadmap Phase 11 Flow eksplisit cuma bilang "Reading Result → Write Reflection → Save → Journal", jadi ini bukan pelanggaran scope — tapi UX-nya kurang ideal (reflection idealnya bisa ditulis kapan saja, bukan cuma sesaat setelah reading selesai). Kandidat penyempurnaan di fase mendatang.
- **`js/components/icons.js` bukan dari Master Spec §3** — daftar `js/components/` di spec cuma: app-shell, sidebar, bottom-nav, tarot-card, card-stack, spread-grid, modal, toast, button, empty-state (10 file). `icons.js` (inline-SVG icon module, dipakai `trash`/`bookmark`/dll sejak fase sebelum Phase 10) sudah ada di repo sebelum sesi ini — bukan ditambahkan di Phase 10, cuma di-extend (+1 ikon `star`). `assets/icons/` di spec itu folder aset gambar statis, beda dari modul JS ini. Belum diklarifikasi apakah ini deviasi yang disengaja dev sebelumnya atau perlu diselaraskan balik ke pola asset-based.
- **Search input & filter/sort select di `history.js` masih inline di dalam page**, bukan komponen reusable terpisah — padahal Master Spec §54 (UI Component System) mendaftarkan `SearchInput` dan `Select` sebagai reusable component tersendiri (sejajar dengan `Modal`/`Toast`/`Badge` yang memang sudah jadi file terpisah di `js/components/`). Keputusan sadar: dibiarkan inline dulu (fungsional tidak terpengaruh), belum diekstrak jadi `js/components/search-input.js`/`select.js` karena baru dipakai di 1 tempat (History) — kalau Journal (Phase 11) atau fase lain butuh search/select serupa, saat itu baru layak diekstrak supaya tidak ekstrak prematur untuk 1 use-case.
- **Masih belum diuji sepenuhnya di browser sungguhan** (sandbox tidak punya headless browser) — Phase 3 divalidasi lewat `node --check` + runtime test Node; Phase 4–8 divalidasi lewat `node --check` + runtime test `jsdom` (struktur DOM, atribut, event handler, full user flow Reading→Result→Simpan→History→Detail dengan interpretasi sungguhan). Smoke test manual pertama di browser nyata sudah menangkap & memperbaiki satu bug (lihat "Bugfix" Phase 7 — animasi flip kartu). Yang **sudah** dikonfirmasi jalan benar di browser sungguhan: animasi flip kartu (setelah fix). Yang **masih belum** dilihat langsung: gradient/palet warna, font Fraunces/Public Sans/IBM Plex Mono ter-load benar dari Google Fonts, animasi shuffle 5-kartu, reveal beruntun per section di Result Page, layout responsive di breakpoint sungguhan, dan **seluruh alur Phase 8** (klik Simpan Reading → reload browser sungguhan → cek reading masih ada di History → buka detail → hapus, serta toggle Reduced Motion di Settings) — jsdom memverifikasi localStorage API dipanggil dengan benar, tapi tidak bisa memverifikasi bahwa data BENAR-BENAR bertahan lewat reload sungguhan (jsdom di-buat ulang tiap test, tidak pernah "reload" dalam artian sungguhan) atau bahwa companion CSS rule `[data-reduced-motion="true"]` benar-benar mematikan animasi secara visual. Rekomendasi tetap sama: lanjutkan smoke test manual, sekarang termasuk alur simpan → reload → buka lagi.
- **Reduced Motion toggle di Settings cuma dites lewat `jsdom`, yang tidak menjalankan CSS transition/animation sama sekali** — assertion `test_phase8_storage.mjs` memverifikasi atribut `data-reduced-motion` ter-set dengan benar di `<html>` dan `getSettings()` ter-persist, tapi tidak (dan secara struktural tidak bisa) memverifikasi bahwa companion rule di `reset.css`/`variables.css`/`reading.css` benar-benar mematikan animasi secara visual di browser. Sama seperti flip bug Phase 7 — kelas bug ini kalau ada, cuma bisa ketahuan lewat browser sungguhan.
- Path gambar kartu (`image`) menunjuk ke file `.webp` yang belum ada secara fisik — `tarot-card.js` sudah didesain graceful (lihat "Important Decisions"), tapi aset sungguhan tetap belum ada; belum ada phase khusus yang secara eksplisit menjadwalkan pembuatan asetnya.
- Teks upright/reversed Minor Arcana bersifat templated — cukup untuk fungsi MVP, bisa diperkaya nanti tanpa perubahan struktur.
- Belum ada `manifest.json` / `favicon.ico` (baru dibutuhkan di Phase 24 — PWA).
- Fitur "matikan kartu terbalik" (custom `reversedProbability`) sudah didukung API-nya di `orientation.js`/`tarot-engine.js`, dan `createReading()` di `reading.js` **masih belum** meneruskan opsi ini (masih pakai default 50/50). Settings page sudah ada & fungsional sejak Phase 8, tapi sengaja cuma diisi Reduced Motion — satu-satunya item yang eksplisit disebut Roadmap Phase 8. Menambah toggle ini kapan pun tinggal: (1) tambah field ke `DEFAULT_SETTINGS`/`getSettings()` di `storage.js`, (2) baca di `reading.js` saat `engine.createReading({ ..., reversedProbability })`, (3) tambah baris toggle di `settings.js` mengikuti pola checkbox Reduced Motion yang sudah ada.
- **Reading yang sedang berjalan (belum `completed`) hilang kalau user menutup tab/reload** — belum ada persistence sama sekali (disengaja, itu scope Phase 8 — Local Storage). `abortReading()` dipanggil kalau user membatalkan lewat UI, tapi tidak ada penanganan untuk unload/reload browser di tengah reading.
- **Result Page hanya bisa menampilkan reading yang baru saja selesai di sesi browser yang sama** (dibaca dari `js/core/state.js`, bukan dari storage) — refresh halaman `#/result` akan kehilangan datanya dan menampilkan empty state. Ini juga scope Phase 8.
- **Five Card spreads** (`career_path`, `love_reading`) sudah ada lengkap di `data/default-spreads.js` sejak Phase 2 tapi belum dimunculkan sebagai opsi di Reading Setup — Roadmap Phase 5 eksplisit cuma minta One Card & Three Card; kemungkinan masuk penyempurnaan Reading experience di fase berikutnya, atau perlu diklarifikasi apakah termasuk salah satu fase yang sudah ada atau butuh entri roadmap sendiri.
- Bugfix folder `tarot/` → `js/tarot/` (lihat Completed Phase 5) sempat lolos tanpa ketahuan sepanjang Phase 3–4 karena validasi kedua phase itu memanggil modul lewat path absolut manual di script test, bukan lewat resolusi import relatif seperti yang akan dipakai browser sungguhan — dicatat di sini sebagai pengingat: validasi `node --check` + runtime test dengan import manual **tidak** menjamin struktur folder benar, cuma menjamin syntax & logic modul itu sendiri benar.
- **Bug ditemukan & diperbaiki selama Phase 6 (sebelum sempat terkirim), dicatat sebagai pengingat proses:** draft pertama `result.js` memanggil `synthesizeReading({ entries: [...] })` dengan `entries` berbentuk `{ entry, position, card }` (mengikuti struktur internal `result.js` sendiri), padahal `synthesizeReading()` mengharapkan tiap entry berbentuk `{ card, orientation, position }` langsung (`orientation` di top level, bukan nested di `entry.orientation`). `interpretCard()` yang dipanggil di dalamnya pun melempar error karena `orientation` selalu `undefined`, dan Result Page jatuh ke empty state alih-alih menampilkan hasil. Lolos dari `node --check` (murni masalah shape data saat runtime, bukan syntax) dan baru ketahuan dari `test_phase6_flow.mjs` (`jsdom`, mensimulasikan flow sungguhan sampai render Result Page) — bukan dari unit test `interpretation.js` sendiri (yang benar semua karena dites dengan shape data yang sudah benar). Diperbaiki dengan me-map `entries` ke shape yang benar sebelum dikirim ke `synthesizeReading()`. Pelajaran yang diambil: unit test modul murni tidak cukup untuk menangkap mismatch kontrak antar-modul — perlu ada minimal satu integration test yang benar-benar memanggil page controller end-to-end.
- **Seluruh alur Phase 9 belum dilihat langsung di browser sungguhan** — sama seperti fase-fase sebelumnya, divalidasi lewat `node --check` + runtime test `jsdom`. Yang jsdom **tidak** bisa verifikasi: layout grid `.tarot-card-grid` di breakpoint sungguhan (auto-fill/minmax), hover state kartu (`data-interactive="true"` CSS), transisi `.card-detail__meanings` dari 2 kolom ke 1 kolom di bawah 640px, dan apakah 78 kartu ukuran `size:"sm"` (`clamp(64px, 18vw, 96px)`) masih nyaman dibaca teksnya di layar kecil. Rekomendasi sama seperti fase lain: smoke test manual, kali ini fokus ke scroll/filter grid dan buka beberapa kartu Major + Minor secara acak di halaman detail.
- Path gambar kartu (`image`) yang menunjuk ke `.webp` yang belum ada secara fisik (lihat Known Issues lama) sekarang juga relevan untuk Library — Card Detail menampilkan kartu ukuran `lg`, jadi fallback inisial huruf (`tarot-card.js`, sudah didesain graceful sejak Phase 4) juga akan terlihat lebih jelas di sini sampai aset gambar sungguhan ada.
- Filter Library saat ini cuma Arcana + Suit (sesuai Roadmap Phase 9 eksplisit). Master Spec §29 lebih detail menyebut juga filter "Number" — belum diimplementasikan karena tidak ada di Roadmap Phase 9 DONE WHEN; kemungkinan perlu diklarifikasi apakah termasuk penyempurnaan fase ini atau scope terpisah.
- Tidak ada tombol "Reset Filter" eksplisit di halaman Library saat filter/search tidak menghasilkan apa pun — user perlu mengosongkan search box / mengembalikan select secara manual. Bukan bug, tapi UX kecil yang mungkin layak ditambah nanti.
- **Seluruh alur Phase 10 belum dilihat langsung di browser sungguhan** — sama seperti fase-fase sebelumnya, divalidasi lewat `node --check` + runtime test `jsdom`. Yang jsdom **tidak** bisa verifikasi: transisi warna/fill tombol favorit (`.history-favorite-btn.is-favorite`), layout toolbar di breakpoint mobile sungguhan (`.history-filters` stack vertikal < 640px), dan apakah batas filter Today/Week/Month "terasa benar" di sekitar tengah malam / pergantian minggu (jsdom cuma diverifikasi terhadap `new Date()` sungguhan saat test dijalankan, bukan kasus edge waktu nyata).
- Tidak ada tombol "Reset Filter"/"Hapus Pencarian" eksplisit di History saat kombinasi search+filter tidak menghasilkan apa pun — sama seperti catatan Library di atas, UX kecil yang belum ditambahkan.
- Sort di History cuma dua arah (Terbaru/Terlama, berbasis tanggal) karena Roadmap Phase 10 tidak merinci sumbu sort lain; kalau nanti perlu sort berdasarkan jumlah kartu atau nama spread, perlu diklarifikasi dulu apakah termasuk scope fase ini atau penyempurnaan terpisah.
- Favorite toggle saat ini cuma ada di list item `history.js` (sesuai teks Roadmap Phase 10: "...dan Favorite ke `history.js`") — belum ada tombol favorit di `history-detail.js` (halaman detail reading). Bisa ditambah kapan pun dengan pola yang sama (baca `reading.isFavorite`, panggil `setGuestReadingFavorite()`), belum dikerjakan karena tidak eksplisit disebut di Roadmap/PROJECT_STATUS Phase 10.

## Next Phase
**Sebelum mulai coding Phase 13**: jalankan `supabase/migrations/0002_seed_system_data.sql` (baru, lihat Known Issues) di Supabase Dashboard project asli Orias — tanpa ini, testing Guest Migration akan gagal di step insert `readings` (FK ke `spreads`/`tarot_cards` yang masih kosong). `0001_init_schema.sql` + auth pipeline sudah diverifikasi Orias, jadi ini satu-satunya blocker tersisa sebelum coding dimulai.

Phase 13 — Authentication (Roadmap Phase 13) — User account: Login, Register, Logout, Forgot Password, Session Persistence, Guest Migration. Flow eksplisit: Guest Reading → Login → Detect Local Data → Migrate → Cloud Reading → Clear Migrated Data. `js/services/auth-service.js` (Phase 12) sudah punya primitif signUp/signIn/signOut/session — Phase 13 tinggal bangun UI (Login/Register/Forgot Password page, form) di atasnya, plus logic migrasi dari `js/core/storage.js` guest-mode (readings, journal) ke tabel Supabase yang sepadan (resolve `spread_positions` lewat `position_index`, bukan slug — lihat catatan Phase 12). DONE WHEN: Guest → Account migration berjalan tanpa kehilangan data.

Rencana implementasi kasar (belum dikerjakan, disusun sesi ini):
- `js/pages/login.js`, `register.js`, `forgot-password.js` — routes baru `/login`, `/register`, `/forgot-password` (Master Spec §4).
- `js/services/auth-service.js` — tambah `resetPasswordForEmail(email)` (belum ada, dibutuhkan halaman Forgot Password).
- `js/services/migration-service.js` (baru) — `migrateGuestDataToCloud(userId)`: baca `listGuestReadings()`/`listGuestJournalEntries()` (storage.js), insert ke `readings`→`reading_cards`→`journals` sesuai urutan FK, baru `remove()` local storage kalau SEMUA berhasil (jangan hapus data lokal kalau migrasi partial/gagal — DONE WHEN eksplisit minta "tanpa kehilangan data").
- `js/app.js` — restore session saat boot (`getSession()`/`getCurrentUser()` → `patchState("user", ...)`) untuk Session Persistence; panggil migration setelah login/register sukses.
- Tempat UI untuk status login & tombol Logout: `js/pages/settings.js` (bukan `js/pages/profile.js` — itu skeleton yang sengaja ditandai "Phase 18" di komentarnya sendiri, jangan disentuh sebelum saatnya).
